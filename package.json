{
  "name": "shopify-backend-service",
  "version": "1.0.0",
  "description": "Backend service for Shopify Admin API integration, webhooks, and business logic.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "test:sync": "deno test supabase/functions"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
